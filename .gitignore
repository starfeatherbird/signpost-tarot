node_modules
dist
.env
.env.local

# 원본 카드 그림(60MB)은 저장소에 넣지 않습니다. 웹용 변환본(public/cards)만 커밋합니다.
card_asset
# 생성물·시안 패키지
docs/screens
*.zip
.claude/

# 스톤 그림 원본 (public/stones 로 변환됨)
stone_source/

# 스톤 가공 작업 폴더 (마스터·검수 시트)
stone_work/
obsidian_reference.png
